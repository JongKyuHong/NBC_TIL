# EDAssetManager & EDGameDataSubsystem을 이용한 플레이어 비동기 로드 구현 가이드

## 📋 개요

이 문서는 `EDAssetManager`와 `EDGameDataSubsystem`을 활용하여 Characters/Player의 비동기 로드와 데이터 캐싱을 구현한 방법을 설명합니다.

---

## 🔄 전체 로딩 흐름

```
로비 데이터 로드 (LobbyReady)
    ↓
InitializeGameData() 호출
    ↓
LoadPhase_UI() → LoadPhase_Item() → LoadPhase_Monster()
    ↓
LoadPhase_Player() 시작
    ├─ PlayerData 에셋 비동기 로드
    ├─ PlayerAnimData 에셋 비동기 로드
    └─ WeaponData 에셋 비동기 로드
    ↓
OnPlayerDataLoaded() → 캐시
    ↓
게임 시작 (PlayerReady → Completed)
```

---

## ✅ 구현 내용

### 1. **EDGameDataSubsystem.h 수정**

#### 1-1) `EDataLoadPhase` Enum 추가

```cpp
UENUM(BlueprintType)
enum class EDataLoadPhase : uint8
{
    // ... 기존 항목 ...
    LoadingPlayer   UMETA(DisplayName = "플레이어 데이터 로딩"),
    // ...
};
```

#### 1-2) Player 데이터 에셋 타입 상수 선언

```cpp
static const FPrimaryAssetType PlayerDataAssetType;
static const FPrimaryAssetType PlayerAnimDataAssetType;
static const FPrimaryAssetType WeaponDataAssetType;
```

#### 1-3) 함수 선언 추가

```cpp
private:
    void LoadPhase_Player();
    void OnPlayerDataLoaded();
```

---

### 2. **EDGameDataSubsystem.cpp 수정**

#### 2-1) 상수 정의

```cpp
const FPrimaryAssetType UEDGameDataSubsystem::PlayerDataAssetType = FPrimaryAssetType(TEXT("PlayerData"));
const FPrimaryAssetType UEDGameDataSubsystem::PlayerAnimDataAssetType = FPrimaryAssetType(TEXT("PlayerAnimData"));
const FPrimaryAssetType UEDGameDataSubsystem::WeaponDataAssetType = FPrimaryAssetType(TEXT("WeaponData"));
```

#### 2-2) `LoadPhase_Player()` 구현

```cpp
void UEDGameDataSubsystem::LoadPhase_Player()
{
	SetPhase(EDataLoadPhase::LoadingPlayer);
	UEDAssetManager& AM = UEDAssetManager::Get();

	// PlayerData, PlayerAnimData, WeaponData 모두 로드
	TArray<FPrimaryAssetId> PlayerIds;
	AM.GetPrimaryAssetIdList(PlayerDataAssetType, PlayerIds);

	TArray<FPrimaryAssetId> PlayerAnimIds;
	AM.GetPrimaryAssetIdList(PlayerAnimDataAssetType, PlayerAnimIds);

	TArray<FPrimaryAssetId> WeaponIds;
	AM.GetPrimaryAssetIdList(WeaponDataAssetType, WeaponIds);

	// 모든 ID를 하나의 배열로 통합
	TArray<FPrimaryAssetId> AllIds;
	AllIds.Append(PlayerIds);
	AllIds.Append(PlayerAnimIds);
	AllIds.Append(WeaponIds);

	if (AllIds.IsEmpty())
	{
		UE_LOG(LogTemp, Warning, TEXT("[EDGameDataSubsystem] 플레이어 관련 데이터 에셋을 찾을 수 없습니다"));
		OnPlayerDataLoaded();
		return;
	}

	// 핸들은 우선 PlayerData로 통일
	TSharedPtr<FStreamableHandle> Handle = AM.LoadPrimaryAssetsAsync(
		AllIds,
		{PlayerDataAssetType.GetName()},
		FStreamableDelegate::CreateUObject(this, &UEDGameDataSubsystem::OnPlayerDataLoaded)
	);

	// 언로드시 키 일치위해서 핸들은 타입별로 저장
	PhaseHandles.Add(PlayerDataAssetType.GetName(), Handle);
	PhaseHandles.Add(PlayerAnimDataAssetType.GetName(), Handle);
	PhaseHandles.Add(WeaponDataAssetType.GetName(), Handle);
}
```

#### 2-3) `OnPlayerDataLoaded()` 콜백 구현

```cpp
void UEDGameDataSubsystem::OnPlayerDataLoaded()
{
	CacheLoadedAssets(PlayerDataAssetType);
	CacheLoadedAssets(PlayerAnimDataAssetType);
	CacheLoadedAssets(WeaponDataAssetType);
	SetPhase(EDataLoadPhase::Completed);
	// 완료 신호
	OnAllDataLoaded.Broadcast();
}
```

---

### 3. **EDPlayerDataAsset.h 수정**

클래스명을 `UPlayerDataAsset` → `UEDPlayerDataAsset`로 정상화

---

### 4. **DefaultGame.ini 수정**

AssetManager 설정에 3가지 타입 등록:

```ini
+PrimaryAssetTypesToScan=(PrimaryAssetType="PlayerData",AssetBaseClass="/Script/EternalDreams.EDPlayerDataAsset",bHasBlueprintClasses=False,bIsEditorOnly=False,Directories=((Path="/Game")),SpecificAssets=,Rules=(Priority=0,ChunkId=-1,bApplyRecursively=True,CookRule=Unknown))
+PrimaryAssetTypesToScan=(PrimaryAssetType="PlayerAnimData",AssetBaseClass="/Script/EternalDreams.EDPlayerAnimDataAsset",bHasBlueprintClasses=False,bIsEditorOnly=False,Directories=((Path="/Game")),SpecificAssets=,Rules=(Priority=0,ChunkId=-1,bApplyRecursively=True,CookRule=Unknown))
+PrimaryAssetTypesToScan=(PrimaryAssetType="WeaponData",AssetBaseClass="/Script/EternalDreams.EDWeaponDataAsset",bHasBlueprintClasses=False,bIsEditorOnly=False,Directories=((Path="/Game")),SpecificAssets=,Rules=(Priority=0,ChunkId=-1,bApplyRecursively=True,CookRule=Unknown))
```

---

### 5. **EDPlayerCharacter.h 수정**

비동기 로드 메서드 2개 선언, 서브시스템 캐시

```cpp
private:
	// 플레이어 데이터 로드 및 초기화
	UFUNCTION()
	void SetupFromPlayerData();
	void ApplyPlayerDataAsset();
	TWeakObjectPtr<UEDGameDataSubsystem> CachedDataSubsystem;
```

---

### 6. **EDPlayerCharacter.cpp 수정**

#### 6-1) BeginPlay() 수정

기존의 `Weapon Test` 코드를 제거하고, `SetupFromPlayerData()` 호출로 대체:

```cpp
void AEDPlayerCharacter::BeginPlay()
{
    // ... 설정 ...

    // 플레이어 데이터 비동기 로드 (스켈레탈 메시, 애니메이션 블루프린트)
    UEDGameDataSubsystem* DataSubsystem = UEDGameDataSubsystem::Get(GetWorld());
	if (!DataSubsystem)
	{
		UE_LOG(LogTemp, Error, TEXT("[PlayerChar] BeginPlay - GameDataSubsystem 없음"));
		return;
	}
	// 데이터 서브시스템 캐싱
	CachedDataSubsystem = DataSubsystem;
	if (DataSubsystem->IsDataReady())
	{
		UE_LOG(LogTemp, Log, TEXT("[PlayerChar] 데이터 준비 완료 - 즉시 초기화 실행"));
		SetupFromPlayerData();
	} else
	{
		UE_LOG(LogTemp, Log, TEXT("[PlayerChar] 데이터 준비 미완료 - 콜백 초기화 실행"));
		DataSubsystem->OnAllDataLoaded.AddDynamic(this, &AEDPlayerCharacter::SetupFromPlayerData);
	}

    // ... ASC Callback 설정 ...
}
```

#### 6-2) `SetupFromPlayerData()` 구현

- EDGameDataSubsystem에서 PlayerData 에셋 조회
- 캐싱데이터 있는지 확인하고 있으면 ApplyPlayerDataAsset() 호출

#### 6-3) `ApplyPlayerDataAsset()` 구현

- 캐싱 데이터 적용
- 스켈레탈 메시 적용
- 애니메이션 블루프린트 적용
- 무기 스폰 및 장착 (기존 로직 유지)

---

## 🎯 주요 변경 사항 요약

| 항목               | 변경 전                        | 변경 후                           |
| ------------------ | ------------------------------ | --------------------------------- |
| 플레이어 에셋 로드 | 동기 로드 (BeginPlay에서 즉시) | 비동기 로드 (EDAssetManager 활용) |
| 에셋 참조 방식     | 하드 레퍼런스                  | 소프트 레퍼런스 (TSoftObjectPtr)  |
| 데이터 구성 방식   | 각각 독립적 로드               | EDGameDataSubsystem 통합 관리     |
| 메모리 효율        | 낮음 (즉시 로드)               | 높음 (필요할 때만 로드)           |
| 캐싱               | 없음                           | DataCache 맵에 저장               |

---

## 📊 데이터 흐름 다이어그램

```
EDGameDataSubsystem::Initialize()
    ↓
LoadPhase_Lobby() → LobbyReady 상태에서 대기
    ↓
InitializeGameData() 호출 (게임 시작 시 외부에서)
    ↓
LoadPhase_UI() → LoadPhase_Item() → LoadPhase_Monster() → LoadPhase_Player()
    ├─ PlayerData 비동기 로드
    ├─ PlayerAnimData 비동기 로드
    └─ WeaponData 비동기 로드
    ↓
OnPlayerDataLoaded() → DataCache에 캐싱 → Completed
    ↓
OnAllDataLoaded.Broadcast()
    ↓
AEDPlayerCharacter::SetupFromPlayerData() 실행
    ↓
ApplyPlayerDataAsset() → 스켈레탈 메시 / 애님BP / 무기 적용
```

---

## ⚠️ 주의사항

1. PlayerData 조회 방식
   GetPrimaryAssetId()는 인스턴스 함수라 키로 사용 불가합니다. 현재는 FPrimaryAssetId를 직접 지정하거나, 캐릭터 선택 시스템 도입 시 GameInstance/PlayerState에 선택된 Id를 저장해서 넘기는 방식으로 확장해야 합니다.

2. HasAuthority() 체크
   ApplyPlayerDataAsset()은 서버에서만 실행됩니다. 클라이언트로만 실행하면 메시가 적용되지 않으니 멀티플레이 테스트 시 주의가 필요합니다.
