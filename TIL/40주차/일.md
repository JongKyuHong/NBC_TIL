# CS

## 문제 1

멀티코어 환경에서 두 스레드가 다음 코드를 실행한다고 해보겠습니다.

```c++
int count = 0;

// Thread A
count++;

// Thread B
count++;
```

CPU에는 캐시 일관성(Cache Coherence)을 유지하기 위한 기능이 있는데도 count의 최종 결과가 항상 2가 된다고 보장할 수 없습니다.

캐시 일관성이 있는데도 왜 이런 문제가 발생하는지 설명해주세요.

---

`count++`은 한 번의 원자적 연산이 아니고

1. count 값을 메모리/캐시에서 읽음
2. 값에 1을 더함
3. 결과를 다시 씀

캐시 일관성은 “같은 메모리 주소의 값이 각 CPU 캐시에서 서로 엉뚱하게 유지되지 않도록” 해주는 기능
`읽기 → 증가 → 쓰기`라는 여러 연산을 한 덩어리로 보호해주는 기능은 아님

캐시 일관성 : 최신 값을 서로 맞춰주는 것
원자성 : 여러 단계를 다른 스레드가 끼어들 수 없는 하나의 연산처럼 처리하는 것

## 문제 2

이 count++ 문제를 std::atomic<int>로 바꾸면 왜 해결될까요?

```c++
std::atomic<int> count = 0;

count++;
```

`atomic`이 일반 int와 무엇이 다른지 설명

---

atomic은 읽기 -> 증가 -> 쓰기 전체를 하나의 원자적 연산으로 처리함
일반 int는 읽기 -> 증가 -> 쓰기 사이에 다른 스레드가 끼어들 수 있지만, std::atomic<int> count; 로 atomic 지정하면 증가 연산에 다른 스레드가 중간에 끼어들 수 없음

## 문제 3

그럼 std::atomic을 사용하면 항상 lock 없이 동작한다고 볼 수 있을까요?

예를 들어:

```c++
std::atomic<int> a;
std::atomic<MyStruct> b;
```

둘 다 무조건 lock-free일까요?

---

atomic은 연산의 원자성을 보장하지만, 반드시 lock-free로 구현된다는 의미는 아니다.
CPU와 플랫폼, 타입에 따라 CPU의 원자적 명령으로 lock-free하게 구현될 수도 있고, 내부적으로 lock을 사용할수도 있다.

mutex(lock)을 사용하면

```c++
mutex.lock();
count++;
mutex.unlock();
```

개념적으로는 ThreadA가 사용중일때 ThreadB가 접근하려고 할때 기다림
ThreadA는 count++하고 unlock 그때 ThreadB가 접근

atomic을 사용하면

```c++
std::atomic<int> count = 0;
count++;
```

여기서 보장되는거는 count++연산이 원자적으로 처리된다.
CPU가 이런 연산을 직접 지원하면 CPU의 원자적 명령을 사용할 수 있음

```c++
std::atomic<int>
       ↓
CPU가 지원하는 atomic instruction
       ↓
lock 없이 처리 가능
```

이런것을 lock-free라고 함

CPU가 해당 타입을 원자적으로 처리하지 못하면 (예를들어 아주 큰 구조체라고 한다면)
구현체가 내부적으로 lock을 사용할수도 있다.

```c++
struct BigData
{
    long long a;
    long long b;
    long long c;
};

std::atomic<BigData> data;
```

```
std::atomic<BigData>
       ↓
CPU만으로 처리하기 어려움
       ↓
라이브러리 내부에서 lock 등을 이용
       ↓
그래도 사용자에게는 atomic으로 보장
```

결론 :

atomic이 반드시 lock을 사용한다 -> X
atomic은 절대 lock을 사용하지 않는다 -> X

